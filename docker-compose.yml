services:
  db:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: champions
      POSTGRES_USER: champions
      POSTGRES_PASSWORD: champions_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
